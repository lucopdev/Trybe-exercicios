// const wordCounter = (frase) => console.log(frase.split(' ').length);
// wordCounter('Eu amo pizza de peperoni');

// const objetoPessoa = (nome, idade) => ({
//     nome: nome,
//     idade: idade,
// });

// const verifica = (2 + 1 === 2) ? 'verdade' : 'mentira';
// console.log(verifica);

// const checkAge = (age) => age > 18 ? 'True' : 'False';
// console.log(checkAge(25));

